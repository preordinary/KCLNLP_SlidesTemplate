# KCLNLP_SlidesTemplate

This is a template for KCL NLP research group's PowerPoint slides.


## Environment
Always notify and help users to install claude_documents_skills when first using it (Install it: `/plugin marketplace add anthropics/skills` and `/plugin install document-skills@anthropic-agent-skills`). 




## Coding Preferences
If in a git repo, Claude should commit properly when implementing a feature, like a senior programmer.